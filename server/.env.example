# Database URL (SQLite for local, PostgreSQL for production)
DATABASE_URL=sqlite:///./homecast.db

# Redis URL for cross-instance WebSocket routing (leave empty for local-only mode)
# Format: redis://HOST:PORT or redis://:PASSWORD@HOST:PORT
REDIS_URL=

# JWT Configuration
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=168

# Server Configuration
PORT=8080

# CORS Origins (comma-separated)
ALLOWED_CORS_ORIGINS=http://localhost:3000,https://homecast.cloud,https://www.homecast.cloud,https://fc18532a-51b0-461b-8f4d-d9ab847d3c95.lovableproject.com,https://homecast.lovable.app,https://preview--homecast.lovable.app
